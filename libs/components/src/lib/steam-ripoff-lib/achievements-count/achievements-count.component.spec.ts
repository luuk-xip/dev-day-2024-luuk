import { AchievementCountComponent } from './achievements-count.component';

describe('component', () => {
  it('should work', () => {
    expect(AchievementCountComponent).toEqual('Component');
  });
});
